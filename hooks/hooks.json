{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "mcp__rinda-cli",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/bin/rinda-cli auth ensure-valid"
          }
        ]
      }
    ]
  }
}
